#include <bits/stdc++.h>
using namespace std;
#define ll long long

class Solution {

    public:
    int RDSA(vector<int>& arr){
        set<int> st;
        for (auto it : st){
            st.insert[arr[it]];
        }
        return st;
    }    
  };

int main() {
    int t;
    cin >> t;
    while (t--) {
        long long N;
        cin >> N;
        Solution ob;

    }
    return 0;
}